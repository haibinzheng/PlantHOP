# Script inventory

The scripts in this directory retain versioned names so that supplementary tables and manuscript methods can be traced to the corresponding analysis stages.

Scripts that provide `--help` or positional command-line interfaces should be preferred for new runs. Relative defaults assume execution from the repository root and local inputs under `data/`. No script should be run against source data with write permissions.

